<%= form_for(objective_seminar) do |f| %>
  <% if objective_seminar.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(objective_seminar.errors.count, "error") %> prohibited this objective_seminar from being saved:</h2>

      <ul>
      <% objective_seminar.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
