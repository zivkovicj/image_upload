<%= form_for(objective_user) do |f| %>
  <% if objective_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(objective_user.errors.count, "error") %> prohibited this objective_user from being saved:</h2>

      <ul>
      <% objective_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
