<p class="btn btn-lg btn-primary remove_btn" id="delete_<%= @seminar.id %>">Delete This Class</p>
<%= link_to "Click here to confirm that you want to delete #{@seminar.name}", @seminar, 
    :method => :delete, :id => "confirm_delete_#{@seminar.id}", class: "btn btn-lg btn-primary confirm_button" %>