<% provide(:title, "#{@market_name}") %>
<% provide(:headerText, "#{@market_name}") %>
<% provide(:headerPic, 'objectives.png') %>

<div class="row">
  <div class="col-sm-6">
    <%= render 'shared/headline' %>
  </div>
  <div class="col-sm-6">
    <%= render 'shared/back-link' %>
    <% if current_user.type == "Student" %>
      <div class="singleOption">
        <h3>You have <span class="singleOption" id="current_cash"><strong><%= @bucks_current %></strong></span> <%= @school.school_currency_name.pluralize(@bucks_current) %></h3>
      </div>
    <% end %>
  </div>
</div>

<%= render 'commodity_list' %>
</br>

<% if @edit_commodity_permission %>
  <%= link_to "Create a New Item", new_commodity_path(:"#{@source_type}_id" => @source.id), :class => "btn btn-primary" %>
<% end %>