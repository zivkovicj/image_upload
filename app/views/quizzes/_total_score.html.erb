

<div class="singleOption container">
    <div class="row">
        <div class="col-sm-4">
            <h2>This Score: <%= @new_total %> %
            </br><strong><%= pluralize(@these_stars, 'Star') %></stong></h2>
            
            </br>
            <h5>Your previous score<br>
            <%= pluralize(@old_stars, 'Star') %></h5>
        </div>
        
        <div class="col-sm-6">
            <h1>New Stars</h1>
            <% 10.times do |x| %>
                <% pic = @quiz.added_stars > x ? "stars/filled_star.png" : "stars/empty_star.png" %>
                <%= image_tag (pic) %>
            <% end %>
            </br>
            
            
            <h2><%= pluralize(@quiz.added_stars, 'Star') %></h2>
        </div>
    </div>
</div>
</br>